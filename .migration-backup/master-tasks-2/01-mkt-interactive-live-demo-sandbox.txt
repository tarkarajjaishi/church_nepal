[MKT] Interactive live demo sandbox
Goal: Let visitors try a read-only Master Control admin without signing up.
Files (repo-root-relative): control-plane/nextjs/app/demo/page.tsx; control-plane/nextjs/app/demo/layout.tsx
Implement:
  - A /demo route rendering a realistic, read-only clone of the admin dashboard with SAMPLE data (churches, MRR, charts).
  - A dismissible banner 'You're in a live demo -- Sign up to go live'. All mutations show a 'demo mode' toast instead of writing.
  - Sidebar + a few key pages (dashboard, churches list, analytics) navigable.
Verify: Visiting /demo shows a working admin UI with sample data; edits are blocked with a demo toast.

CONTEXT: This is churchnepal.com -- the Master Control SaaS site. Frontend: control-plane/nextjs
(Next 16, React 19, App Router, Tailwind, CSS-variable theming). Backend: control-plane/backend (Rust/Axum/SQLx).
DESIGN SYSTEM: "use client" for interactive files. Colors ONLY via CSS vars (never hardcode):
--bg --panel --panel-2 --panel-3 --border --border-soft --text --text-strong --muted --accent
--accent-2 --accent-soft --gold-soft --good --good-soft --max  (e.g. bg-[var(--panel)] text-[var(--muted)]).
UI kit: @/components/ui/{card,badge,button,input}. Marketing pages wrap in PublicLayout (import from the
right relative depth). Admin cards: "card bg-[var(--panel)] border border-[var(--border)] rounded-xl p-5".
No new npm deps; inline SVG icons. Fully responsive; works light + dark automatically. Must compile (valid TS/TSX).
